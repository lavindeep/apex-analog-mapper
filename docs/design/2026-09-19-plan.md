# Rewrite implementation plan

Companion to `2026-09-19-rewrite.md` (the design). Working plan for the `claude/rewrite`
branch. Revised after the plan review recorded in `reviews/plan.md`.

## Conventions

- Branch `claude/rewrite`, one PR to `main` at the end. Conventional commits, author
  Lavindeep Dhillon, explicit `git add` paths.
- .NET 10 SDK, portable install at `C:\Users\lavin\Documents\Codex\tools\dotnet`
  (prepended to PATH per shell, never machine-wide).
- `dotnet test -c Release` runs everything that needs no hardware. Hardware tests carry
  `[HardwareFact]`, which skips unless `APEX_HW_TESTS=1`. The 30-minute soak carries
  `[SoakFact]`, which skips unless `APEX_SOAK=1`.
- Nothing in the hook callback, sensor loop, or engine tick allocates. Tests assert
  zero allocations with `GC.GetAllocatedBytesForCurrentThread`, read on the thread
  under test after a warm-up pass.
- Hardware test thresholds live in `tests/ApexMapper.Windows.Tests/HardwareThresholds.cs`
  and are filled from `docs/design/measurements.md` with stated headroom. Stage 7
  checks the two agree.
- `docs/design/requirements.md` lists every design requirement as a numbered line with
  an owning stage and an evidence column (`file:line` plus test name). Trace reviewers
  fill the column; a requirement with no evidence is a gap.

## Gate protocol

At the end of each stage:

1. Findings first. Reviewers run in parallel, each with the design, the plan,
   `requirements.md`, and the stage diff. Each must run `dotnet test -c Release`, paste
   the output, and cite `file:line` for every finding. Findings without a citation are
   discarded. Findings are committed to `docs/design/reviews/stage-N.md` before any fix.
2. Reviewers on every stage: trace (fills the evidence column, lists gaps), failure
   (every thread, lock, handle, and failure path: races, leaks, deadlocks, silent
   swallows), test (names behaviours the tests do not pin). Plus the stage-specific
   reviewer named below.
3. Fixes land in their own commits. A second pass reviews only the fix commits.
4. Hardware tests that apply to the stage run on the maintainer's PC and their output
   is saved under `docs/design/reviews/stage-N/`. Stages with no hardware tests say so.
5. Blockers and highs fixed before the stage closes. Mediums fixed or recorded with a
   reason.

## Stage 0: reset, toolchain, measurements

### 0a. Reset the tree

- `git rm -r` everything except `.git` and `docs/design`. (There is no `LICENSE` in
  either tree; the README claimed MIT.)
- New root files: `LICENSE` (MIT, Lavindeep Dhillon), `.gitignore`, `.editorconfig`,
  `Directory.Build.props` (nullable, implicit usings, warnings as errors with a
  `NoWarn` list for XAML-generated and WPF-UI warnings, `LangVersion` latest; the
  target framework is per project: `net10.0` for Core and its tests, `net10.0-windows`
  for Windows, App, and their tests), `Directory.Packages.props`, `ApexAnalogMapper.sln`,
  `README.md` placeholder pointing at the design, `SECURITY.md` carried over with the
  supervisor and driver-download paragraphs removed.
- Projects: `src/ApexMapper.Core`, `src/ApexMapper.Windows`, `src/ApexMapper.App`,
  `tests/ApexMapper.Core.Tests`, `tests/ApexMapper.Windows.Tests`,
  `tests/ApexMapper.App.Tests`, `spike/Spike` (kept until stage 7).
- Before deleting the old workflows, copy the version-from-tag and `SHA256SUMS.txt`
  logic out of `.github/workflows/release.yml` into `docs/design/notes-release.md`.
- Write `docs/design/requirements.md`.

### 0b. Toolchain proof

Done before the spike, because it is the only thing that can force a stack change.

- `ApexMapper.App` is a hello-world WPF-UI `FluentWindow` with the OS theme applied and
  `VelopackApp.Build().Run()` in a custom `Main`. Confirm WPF-UI loads on .NET 10; the
  fallback is plain WPF with a hand-written Fluent-style resource dictionary.
- `scripts/pack.ps1`: `dotnet publish src/ApexMapper.App -c Release -r win-x64
  --self-contained`, then `vpk pack --packId ApexAnalogMapper --packVersion <v>
  --packDir <publish> --mainExe ApexAnalogMapper.exe`. Run it locally, install the
  `Setup.exe`, confirm the Add or Remove Programs entry, uninstall.
- `.github/workflows/ci.yml`, triggered on `push` to `main` and `claude/**` and on
  `pull_request`: `actions/setup-dotnet` with `10.0.x`, `dotnet tool install -g vpk`,
  restore, build, `dotnet test -c Release`, `scripts/pack.ps1` with version
  `0.0.0-ci.${{ github.run_number }}`, upload the package with `retention-days: 7`.
  Must be green before the spike starts.
- Decision recorded here: CI does not attempt to install ViGEmBus. The loopback, kill,
  wedge, and soak tests run only on the maintainer's PC, so a regression in them is
  invisible to CI until the next hardware gate.

### 0c. Measurement spike

`spike/Spike` on `net10.0-windows`, references HidSharp and Nefarius.ViGEm.Client. No
tests, no abstractions. Each subcommand writes CSV under `spike/out/` (git-ignored)
and prints a summary.

| Command | What it does | Output |
| --- | --- | --- |
| `probe` | Enumerates HID devices with VID 0x1038: product id, usage page and usage per interface, report lengths, container id. Opens the 0xFFC0 interface, sends 0x90, prints the firmware string. Dumps the raw 65-byte replies for all five groups at rest and with W held into `docs/design/fixtures/` as hex text files. | Device table, firmware, fixtures for the Core protocol tests. |
| `cycle --seconds 30` | Polls groups 2 and 3 back to back with the queue drained before each write. Records per-exchange latency and per-cycle period. Records which `SteelSeries*` processes are running. | p50, p99, max for exchange and cycle; fault count. Run twice: with GG and its engine service running, and with the engine service stopped (`Stop-Service` from an elevated prompt, GUI closed, verified with a process list; restarted afterwards and the board's actuation settings confirmed unchanged). If elevation is unavailable, ask the maintainer to close GG and record the state. |
| `step --seconds 20` | Polls group 2 at maximum rate while the user taps W repeatedly as fast as possible. Logs raw and filtered W with timestamps. | Achieved sample period. Per press: 10 to 90 percent rise time of raw and filtered, lag of filtered behind raw in samples and ms. Noise at rest for each, peak to peak in counts. Report both, because noise versus lag is the tradeoff. |
| `tick --seconds 10` | Runs a 1 ms loop three ways: `Thread.Sleep(1)`, `WaitHandle.WaitOne(1)`, and a `CREATE_WAITABLE_TIMER_HIGH_RESOLUTION` timer. | Tick period histogram per method: p50, p99, max. |
| `readback --seconds 10` | Control run. Connects a pad, submits a known step every 2 ms, reads it back from a second thread using the timer method that won `tick`. | p50, p99, max submit-to-readback with no keyboard involved. This is the floor; the sampler's own period distribution is reported beside it. |
| `e2e --seconds 30` | Connects a pad. Polls group 2 and writes W depth to the right trigger. A hook timestamps W down without swallowing. A second thread samples `XInputGetState` with the winning timer. | Headline: sensor-crossing-threshold timestamp to first non-zero trigger readback, p50, p99. Secondary, labelled "digital actuation offset": hook key-down timestamp to the same readback, with the GG actuation setting in mm recorded beside it. |
| `rest --minutes 10` | Polls groups 2 and 3 with all keys released. | Per key: mean, peak to peak, min, max, drift of a 5 s moving mean from first to last, in counts. |
| `travel` | Prompts: release W, press W fully, then press W slowly until the digital key-down arrives (Raw Input). Repeats for A, S, D. | Rest count, full-press count, span, and the count at which GG's digital actuation fires. |
| `kill` | Parent spawns itself with `--child`. Child: connects a pad, sets right trigger 255, installs a hook that swallows W including injected events (test-only mode), pumps messages on the hook thread, signals ready, and blocks a different thread. Parent: creates a real top-level window, brings it to the foreground, waits 1 s, `TerminateProcess` on the child, then polls `XInputGetState` every 10 ms for 3 s and sends a W with `SendInput`, watching for `WM_KEYDOWN` on its window. | Time from kill to `ERROR_DEVICE_NOT_CONNECTED` on the pad's former index; whether the W arrived. Contingency: if the pad lingers, the design gains a minimal watchdog process that waits on the app's process handle and unplugs, with no protocol. |

Deliverable: `docs/design/measurements.md` with every number and the decisions they
settle:

1. Raw or filtered bytes for depth.
2. Freshness constants and sensor loop pacing (back to back, or a floor).
3. Engine timer method and the achievable tick period.
4. Whether process death unplugs the pad, or the contingency applies.
5. Values for `HardwareThresholds.cs`: readback floor plus headroom, watchdog bound
   (200 ms staleness + timer period + measured unplug time + margin), kill bound.

The response representation is decided now, not fitted: exponent plus saturation
point plus deadzone (three floats), because they serialise and edit better than point
lists. Whether the maintainer can feel the difference from the old point-list curve is
a stage 5 question with the live preview.

Gate: one Opus reviewer checks the measurement method and the conclusions drawn.

## Stage 1: Core

### Interfaces

Fixed before any file is written. The feel reviewer checks the worked example against
the code.

```csharp
// Per slot: digital state from the hook, analog depth from the sensor, gate bit.
// Two values, because fallback needs both at the same instant.
public sealed class KeyStateStore
{
    public void SetDigital(int slot, bool down);        // hook thread
    public void SetAnalog(int slot, float depth);       // engine thread, NaN = unavailable
    public void Gate(int slot);  public void GateAll();
    public bool IsGated(int slot);
    public KeySlot Read(int slot);                      // digital, analog, gated
}

// One per analog key. Owns the at-rest latch and the fallback ramp.
public struct SourceSelector
{
    public float Select(in KeySlot slot, bool analogFresh, bool calibrated,
                        float dtMs, float fallbackRampMs, out bool usingAnalog);
}

public sealed class Mapper
{
    public Mapper(CompiledProfile profile);              // bindings + calibration + response, immutable
    public void Tick(KeyStateStore store, SensorSnapshot snapshot, long nowTicks,
                     float dtMs, ref PadReport report);
}
```

Tick order per binding:

1. Gate. If the slot is gated, the key's selector, ramp, and rate-mode state are reset
   to zero and the key contributes zero. For an analog-driven key the gate clears only
   on an analog reading at rest (rapid trigger makes hook key-ups mean "rising"); for
   a digital key it clears on the hook's key-up. The store therefore records, per
   slot, whether the profile drives it from the sensor.
2. Source select. Analog if the snapshot is fresh (`nowTicks - snapshot.Timestamp`
   under the fixed `SensorSnapshot.FreshnessMs` of 60 ms), the key is calibrated, and
   the at-rest latch is set. Otherwise digital.
3. Ramp. One ramp per key. While analog drives the key, the ramp value is set to the
   depth every tick (so it is always seeded). While digital drives it, the ramp
   integrates toward 0 or 1 at the binding's press or release rate; if the binding's
   rate is zero, the fallback rate (50 ms full scale) applies while fallback is active.
4. Deadzone and response.
5. Conflict rule, then axis mode, for axis pairs.
6. Pack.

Worked example, W bound to right trigger, press ramp 0, analog held at 0.7: sensor
faults, snapshot goes stale. Tick N: source becomes digital (hook says down), ramp is
seeded at 0.7, target 1.0, fallback rate 50 ms full scale, so the output reaches 1.0
after 15 ms. User releases W: hook key-up, ramp runs to 0 at the fallback rate. Sensor
recovers while W is still up: the next snapshot reads W at rest, the at-rest latch is
set, and the next press is analog again. If the sensor recovers while W is still held,
the latch stays clear and W stays digital until it is released once. Recovery
therefore never needs a reverse ramp: it always happens at zero.

Profile changes stop the session; `Mapper` is rebuilt for the next start. There is no
mid-session profile swap.

Digital ownership: the hook owns the store's digital value and gate clearing for
mapped keys. Raw Input is used only for device attribution (the other-keyboard notice)
and for the learn and travel steps. The hook has no device identity, so a key-up from
any keyboard clears the gate; the design records this limitation.

### Files

| File | Responsibility | Key tests |
| --- | --- | --- |
| `Keys/ScanCode.cs` | `readonly record struct ScanCode(ushort Value)`, page helpers, slot index 0..767, reserved key check. | E0/E1 pages distinct; reserved keys identified. |
| `Keys/KeyStateStore.cs` | As above, plus a per-slot "analog-driven" flag set from the profile. Interlocked writes. | Gated slot ignores non-zero analog and digital-down; digital up clears a digital slot; digital up does NOT clear an analog-driven slot; analog at rest clears; two values readable atomically. |
| `Calibration/KeyCalibration.cs` | Rest, full, noise band, sensor index. Validation: span above band plus 100 counts. | Rejects small span; accepts descending travel. |
| `Calibration/Normalizer.cs` | `Depth(raw)` per the design formula; `IsAtRest(raw)`. | Depth at band+1 is near zero (no step); drift of band+1 still at rest under the tolerance; descending. |
| `Response/Response.cs` | Exponent, saturation, deadzone. Presets linear, soft, aggressive. `Map(depth)`. | Monotone; reaches 1 at saturation; presets differ; deadzone re-normalises. |
| `Bindings/PadTarget.cs` | Buttons, triggers, stick axes. | Covers every `PadReport` field. |
| `Bindings/Binding.cs` | `KeyBinding` and `AxisBinding` with response, ramps, conflict rule, axis mode. | JSON round trip. |
| `Engine/Ramp.cs` | Linear ramp, seedable, rate override. | Press, release, interruption, seed, override rate. |
| `Engine/SourceSelector.cs` | As above, with the at-rest latch. | Fault mid-press ramps from last analog value; recovery while held stays digital; recovery at rest resumes analog; uncalibrated never analog; gate resets state. |
| `Engine/Conflict.cs` | Last-input-wins and neutral. | Both held: last wins; release winner: other takes over; neutral centres. |
| `Engine/AxisMode.cs` | Position and rate. Rate mode ships; the default per axis is decided in stage 5's feel step. | Rate: deflection grows with depth times dt, returns to centre on release, clamps. |
| `Engine/PadReport.cs` | Packed report, value equality, packing rules. | Packing table; equality; NaN. |
| `Engine/Mapper.cs` | Tick as above. Allocation-free. | Full-profile tick; zero allocations over 10,000 ticks; gate mid-ramp: gate at 0.6, clear, output rises from 0 rather than snapping to 0.6; alt-tab and start gating. |
| `Sensors/SensorMap.cs` | 70-slot table plus per-keyboard overrides. | 65 supported; arrows unsupported; override wins. |
| `Sensors/SensorRequest.cs` | Struct with a private constructor; only `SensorProtocol.Firmware()` and `SensorProtocol.Group(n)` create one. This is the single allowlist. | No other command byte is constructible; selector range enforced. |
| `Sensors/SensorProtocol.cs` | Pure: build requests, `ParseFirmware`, `ParseGroup(reply, raw|filtered)`, structural checks, single-reply plausibility (all zero, all identical). | Stage 0 fixtures parse; padding and report id checks; plausibility. |
| `Sensors/CycleStats.cs` | Ring of 100 periods for the status card; p50, p99; three consecutive cycles of 100 ms or more signal a fault. | Percentiles; window; one slow cycle is not a fault, three are. |
| `Sensors/SensorSnapshot.cs` | Double-buffered readings: timestamp, per-sensor raw and filtered, which groups were read, generation; the fixed 60 ms `FreshnessMs`. | Freshness against the limit; group offsets; copy. |
| `Sensors/GroupSignature.cs` | The set of absent slots (under 50 at rest) per group, recorded at calibration; `Matches(reply)` also rejects any value above 4095. | Rest fixtures produce the signatures; a group 3 reply fails group 2's signature; a firmware reply fails every signature. |
| `Sensors/KnownKeyboards.cs` | Product id table with verified flag and the verified firmware list. | Both gen 1 ids verified; others not; firmware 4.9.1 verified. |
| `Sensors/LearnStep.cs` | Over a series of snapshots: finds the sensor index that moves most for a prompted key; rejects ambiguity; the change-on-press plausibility check for try-it. | Picks the moving slot; two movers ambiguous; no mover fails plausibility. |
| `Sensors/CaptureExport.cs` | JSON export: product id, firmware, report lengths, container id, replies at rest and per prompted key. | Round trip. |
| `Profiles/Profile.cs`, `ProfileJson.cs`, `DefaultProfiles.cs` | Model, versioned JSON, the Forza default. | Round trip; default validates; unknown fields ignored. |
| `Storage/JsonFile.cs` | Atomic write, single `.bak`, `.corrupt` rename. | Crash between temp and replace leaves primary intact; corrupt primary recovers from bak. |

Stage-specific reviewer: feel reviewer, checks the worked example and the conflict and
gate tables against the code with numbers.

## Stage 2: Windows input

| File | Responsibility | Tests |
| --- | --- | --- |
| `Native/*.cs` | `[LibraryImport]` declarations and structs: user32 hooks and Raw Input, cfgmgr32, kernel32 timers and process, xinput1_4, WinEvent. | `Marshal.SizeOf` per struct against documented x64 sizes (`KBDLLHOOKSTRUCT` 24, `RAWINPUTDEVICE` 16, `RAWINPUTHEADER` 24, `XINPUT_STATE` 16). |
| `Timing/HighResolutionTimer.cs` | Wrapper for the method that won `tick`, with `WaitNext()` and a stop signal. | Hardware: p99 period under the threshold file's value. |
| `Hid/VendorInterface.cs` | Finds the 0xFFC0 interface by container id (exactly one or nothing), opens non-exclusive, 150 ms timeouts, `Exchange(SensorRequest, Span<byte> reply)`. No drain. `Abort()` closes the handle to unblock a pending read. | Fake stream: short reply faults; timeout faults; abort unblocks a 150 ms read within 20 ms. Hardware: firmware string on the real board. Architecture test: only `VendorInterface` calls the HID write. |
| `Hid/SensorPoller.cs` | Thread. States: Stopped, Starting, Running, Faulted (reason), Waiting (no device). Verifies firmware, polls the needed groups back to back, checks every reply against its `GroupSignature`, canary every 50 cycles, publishes `SensorSnapshot` with the fixed freshness, updates `CycleStats`, faults on a read timeout or three consecutive slow cycles, 1 s backoff on a wait handle that Stop signals, reopen and re-verify. | Fake stream: canary mismatch retires; a shifted reply fails its signature and retires; a firmware reply in a sensor slot retires; generation change discards an in-flight reply; one slow cycle stales without faulting; Stop during backoff returns within 20 ms; Stop during a blocked read returns within 50 ms; zero allocations per cycle. Hardware: cycle p99 under `6 x groups + 9 ms`. |
| `Input/RawInputPump.cs` | Own thread, message-only window, `RIDEV_INPUTSINK | RIDEV_DEVNOTIFY`, decodes make codes to `ScanCode`, tags device id, pushes to a lock-free ring, raises device arrival and removal. `Stop()` posts `WM_QUIT` and joins. | Decoder table; ring overflow counted; stop joins within 500 ms. Hardware: `SendInput` W arrives with device id 0. |
| `Devices/KeyboardDiscovery.cs` | Consumes the pump's device events plus an initial enumeration; filters VID 0x1038; container id and friendly name; uses `KnownKeyboards`. | Filters; hardware: finds the connected board. |
| `Input/HookPolicy.cs` | Pure swallow rules per the design, lock-free per-slot state. On foreground loss, marks swallowed-down keys as passed so their eventual key-up passes through; nothing is injected. | Every rule from the design's blocking section; swallowed down then foreground loss then up passes. |
| `Input/KeyboardHook.cs` | Hook thread with message loop, allocation-free callback reading `KBDLLHOOKSTRUCT` by pointer, `HookPolicy`, writes the store's digital state, timestamps into a preallocated ring for off-thread analysis, 50 ms timer slot (watchdog and controller-presence checks live here, foreground is a flag read), Ctrl+Alt+F12 raises `StopRequested` on this thread, above-normal priority. `Stop()` posts `WM_QUIT` and joins. | Hardware: install, swallow a synthetic W in test mode while a test window is foreground, uninstall; callback p99.9 under threshold and excursion count under limit. |
| `Input/ForegroundTracker.cs` | Own thread with message loop. `WINEVENT_OUTOFCONTEXT` foreground hook. Resolves the owning process, unwraps `ApplicationFrameHost`, matches by executable path, detects elevation, publishes a volatile flag and an edge event. | Fake window tree: frame host unwrap; path match survives PID change; elevation flag. |

Stage-specific reviewers: protocol reviewer (desync scenarios, the allowlist boundary,
what a foreign board sees), hook reviewer (callback cost, UIPI, timeout unhooking,
chord handling, what the hook thread may not call).

Hardware tests for the gate: `VendorInterface` firmware, `SensorPoller` cycle stats,
`RawInputPump` synthetic W, `KeyboardHook` install and callback timing,
`HighResolutionTimer` period.

## Stage 3: output and session

| File | Responsibility | Tests |
| --- | --- | --- |
| `Output/ViGEmPad.cs` | Connect with the neutral readback loop, `Submit(in PadReport)` only when changed and at most every 2 ms, `Zero`, `Disconnect` (zero first, idempotent). `PadOwner` interlocked token: whoever claims it may call the driver; the engine checks it before each submit. Driver exceptions surface as `PadFault`. | Fake client: submit only on change; zero before disconnect; a claim by the watchdog stops engine submits. Hardware: connect reads all-zero; full-scale round trip. |
| `Output/DriverStatus.cs` | Running, present but not started, missing. | Hardware: running on this PC. |
| `Session/Session.cs` | State machine per the design table. Sets `GCSettings.LatencyMode = SustainedLowLatency` on start and restores it on every exit path. Profile edit stops the session. | One test per design table row with fakes; ordering: stop during start, unplug during alt-tab, hotkey during backoff. |
| `Session/PowerNotifier.cs` | `SystemEvents.PowerModeChanged`; resume raises Stop. | Fake source: resume stops. |
| `Session/Watchdog.cs` | Runs in the hook thread's 50 ms timer. Armed when Running begins, disarmed when Stopping begins (the engine ticks and pushes zero in Paused, so the watchdog stays armed there). Tick timestamp older than 200 ms: claim `PadOwner`, zero, unplug, unhook. Also checks the pad's XInput slot and raises controller-disconnected. | Fake clock: fires once; not while ticking; not in Starting; controller loss detected. |
| `Session/CrashGuard.cs` | `AppDomain.UnhandledException` and unobserved-task handlers: claim `PadOwner`, zero, unplug, unhook, restore GC mode. | Hardware: kill and wedge tests. |
| `Session/EngineLoop.cs` | Engine thread: timer, apply snapshot, `Mapper.Tick`, submit if owner, publish tick timestamp. | Zero allocations; hardware loopback. |

Shutdown order on Stop, with the bound per step:

1. Set the stopping flag; disarm the watchdog.
2. Engine: signal the timer, join (bound 50 ms).
3. Pad: zero, disconnect (bound 2 s, the driver's own limit).
4. Hook: `WM_QUIT`, join (bound 500 ms).
5. Sensor: signal the stop handle, `Abort()` the interface, join (bound 500 ms).
6. Raw Input pump and foreground tracker: `WM_QUIT`, join (bound 500 ms each).
7. Restore GC mode.

Hardware tests, each `[HardwareFact]`:

- Loopback: synthetic depth ramp on `LeftStickX` into the engine; XInput sampled with
  the winning timer from another thread; asserts `dwPacketNumber` increments per second
  at or above the threshold file's value while input changes every tick, and p99
  submit-to-readback under floor plus headroom.
- Kill: child harness as in the spike (pad at 255, hook swallowing including injected,
  message pump alive, a different thread blocked); parent terminates it; asserts
  `ERROR_DEVICE_NOT_CONNECTED` on the former index within the threshold and that a
  `SendInput` W reaches the parent's foreground window.
- Wedge: child harness blocks inside a fake submit that never returns; asserts the
  watchdog claims the owner, zeros, and unplugs within the derived bound, and that the
  wedged submit's eventual return does not touch the driver again.
- Soak (`[SoakFact]`): 30 minutes of synthetic input; asserts working set flat within
  10 MB, handle count flat, every key zero at the end, `GC.GetTotalPauseDuration`
  delta under 5 ms per wall-clock second, and `GC.CollectionCount(2)` unchanged.

Stage-specific reviewer: safety reviewer, attacks the "what the game sees" column and
the shutdown order.

## Stage 4: persistence and settings

| File | Responsibility | Tests |
| --- | --- | --- |
| `App/Storage/AppPaths.cs` | `%AppData%\ApexAnalogMapper` with `profiles\`, `calibration\`, `settings.json`, `log.txt`. | Paths; never references the old `ApexMapper` folder. |
| `App/Storage/ProfileStore.cs` | List, load, save, delete, reset to default. | Round trip; default seeded; delete keeps default. |
| `App/Storage/CalibrationStore.cs` | Per container id and firmware; mismatch loads with a warning flag. | Mismatch warns, keeps data. |
| `App/Storage/SettingsStore.cs` | Selected keyboard, remembered game path, active profile, prerelease opt-in, update cache. | Round trip; defaults. |
| `App/Logging/FileLog.cs` | Single background writer fed by a channel; rolling 1 MB; session events and faults only. Hook, sensor, and engine threads never call it directly. | Rolls; writer thread is the only file writer. |

No hardware tests. Reviewers: trace, failure, test.

## Stage 5: window

Plain `INotifyPropertyChanged` view models, one per card, each card a `UserControl`.

| Card | View model responsibilities | Tests |
| --- | --- | --- |
| Status | State, reason, Start enabled with the blocking reason and a "go to calibration" action, cycle p50 and p99, submit rate, warnings with reason text. | Disabled reasons; navigation action; fallback warning carries the reason. |
| Keyboard | Discovered boards, persisted selection, verified or try-it, firmware with the verified-list warning. Try-it flow: firmware step, consent with the risk text, plausibility, learn step, banner, capture export. | Selection persists; unverified label; consent gates 0xD7; export round trip. |
| Game | Visible windows with executable path, remembered game auto-selected, elevated warning. | Re-selection; elevated flag. |
| Profile | Profiles, active profile, binding rows with key capture and target picker, response preset and sliders, live preview fed by the bound key's depth, axis mode toggle. | Capture rejects reserved keys; preview follows depth. |
| Calibration | Per analog key: live raw bar, Set released, Set fully pressed, noise at rest, learn step, per-keyboard save. | Set pressed refused when span too small; learn assigns index. |
| Setup | Driver state and browser button (re-check on focus), Steam Input note, update state with Update disabled during a session. | Button disabled while running. |

Feel step, done by the maintainer with the running app at the end of this stage:
drive with position and rate steering, and with each response preset, and record the
chosen defaults for the Forza profile in `measurements.md`.

Stage-specific reviewer: UX reviewer, runs the app on this PC, screenshots every card
in light and dark, reports anything unclear or off the Windows 11 look.

## Stage 6: packaging, update, docs

- `scripts/pack.ps1` finalised; app id `ApexAnalogMapper`, icon.
- `App/Update/UpdateChecker.cs`: `GithubSource`, cached 6 hours, silent on failure,
  prerelease from settings, refuses to apply while a session is active.
- `.github/workflows/release.yml`: on tag `v*`, build, test, pack with the tag version,
  write `SHA256SUMS.txt`, `vpk upload github` with `contents: write`, then
  `gh release edit --notes-file` to append the hashes.
- README, written in the maintainer's voice: why this exists (wanting to use the Apex
  Pro's hall effect switches as analog throttle, brake, and steering in Forza Horizon
  5 and 6, though any game that takes an Xbox controller works), then install and
  size, SmartScreen note with hashes, driver install (official link, no download),
  Steam Input, start before launching the game, the every-keyboard blocking
  limitation, alt-tab held-key note, calibration walkthrough, no keyboard setting
  changes needed (rapid trigger and actuation do not affect analog input),
  troubleshooting, build from source, the never-inject stance. `SECURITY.md` updated.
  `docs/acceptance.md`: the maintainer's in-game checklist.
- Version: this release ships as `0.5.0-alpha.1`, the first real v0.5 alpha. Set in
  `Directory.Build.props` and used by the tag `v0.5.0-alpha.1`.

Stage-specific reviewer: security reviewer (update path, hook, what the installer
touches).

## Stage 7: close

- Review the seams between stages and the gate ledger, not the whole diff at once.
  Fresh clone, build, pack, install, run, uninstall on this PC.
- `HardwareThresholds.cs` matches `measurements.md`.
- Delete `spike/`.
- Open the PR to `main` with the design, measurements, and acceptance checklist
  linked. Register it with the thread. Hand over for Cursor bugbot and physical testing.
