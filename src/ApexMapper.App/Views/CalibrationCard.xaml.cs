using System.Windows.Controls;

namespace ApexMapper.App.Views;

/// <summary>One row per analog key: the live reading, the saved marks, and the calibration steps.</summary>
public partial class CalibrationCard : UserControl
{
    public CalibrationCard() => InitializeComponent();
}
