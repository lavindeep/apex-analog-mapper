using System.Windows.Controls;

namespace ApexMapper.App.Views;

/// <summary>The profiles, the bindings editor with key capture, and the response preview.</summary>
public partial class ProfileCard : UserControl
{
    public ProfileCard() => InitializeComponent();
}
