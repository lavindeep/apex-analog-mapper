<UserControl x:Class="ApexMapper.App.Views.Profiles.ProfileSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock Text="Profile" Style="{StaticResource SectionTitle}" />
        <ComboBox ItemsSource="{Binding Profiles}" SelectedItem="{Binding Selected, Mode=TwoWay}"
                  DisplayMemberPath="DisplayName" Margin="0,9,0,18" AutomationProperties.Name="Profile" />
        <DockPanel Margin="0,0,0,10">
            <Button Content="Edit bindings" DockPanel.Dock="Right" Click="EditBindings" Padding="10,6" />
            <TextBlock Text="Bindings" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center" />
        </DockPanel>
        <ItemsControl ItemsSource="{Binding BindingSummary}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Input}" FontSize="13" TextWrapping="Wrap" Margin="0,0,12,0" />
                        <TextBlock Grid.Column="1" Text="{Binding Output}" Style="{StaticResource Caption}" />
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
