using System.Windows.Controls;

namespace ApexMapper.App.Views;

/// <summary>The plugged-in boards, the firmware, and the try-it flow for an untested model.</summary>
public partial class KeyboardCard : UserControl
{
    public KeyboardCard() => InitializeComponent();
}
