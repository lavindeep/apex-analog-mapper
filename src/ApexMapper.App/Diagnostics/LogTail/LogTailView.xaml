<UserControl x:Class="ApexMapper.App.Diagnostics.LogTail.LogTailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="320" d:DesignWidth="640">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="8,6">
            <CheckBox Content="Debug" IsChecked="{Binding ShowDebug}" Margin="0,0,8,0" />
            <CheckBox Content="Info"  IsChecked="{Binding ShowInfo}"  Margin="0,0,8,0" />
            <CheckBox Content="Warn"  IsChecked="{Binding ShowWarn}"  Margin="0,0,8,0" />
            <CheckBox Content="Error" IsChecked="{Binding ShowError}" Margin="0,0,16,0" />
            <Button Content="Refresh"  Command="{Binding RefreshCommand}"  Margin="0,0,8,0" Padding="8,2" />
            <Button Content="Copy All" Command="{Binding CopyAllCommand}" Padding="8,2" />
        </StackPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Entries}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  Margin="8,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time (UTC)"
                                    Binding="{Binding TimestampUtc, StringFormat={}{0:HH:mm:ss.fff}}"
                                    Width="120" />
                <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="64" />
                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="8,4">
            <TextBlock Text="Malformed lines:" Margin="0,0,4,0" />
            <TextBlock Text="{Binding MalformedCount}" FontWeight="SemiBold" />
        </StackPanel>
    </Grid>
</UserControl>
