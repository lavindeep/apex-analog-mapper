<UserControl x:Class="ApexMapper.App.Diagnostics.Latency.LatencyHistogramView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:oxy="http://oxyplot.org/wpf"
             mc:Ignorable="d"
             d:DesignHeight="240" d:DesignWidth="480">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="8,6">
            <TextBlock Text="P50:" Margin="0,0,4,0" />
            <TextBlock Text="{Binding P50, StringFormat={}{0:F0} µs}" Margin="0,0,16,0" FontWeight="SemiBold" />
            <TextBlock Text="P95:" Margin="0,0,4,0" />
            <TextBlock Text="{Binding P95, StringFormat={}{0:F0} µs}" Margin="0,0,16,0" FontWeight="SemiBold" />
            <TextBlock Text="P99:" Margin="0,0,4,0" />
            <TextBlock Text="{Binding P99, StringFormat={}{0:F0} µs}" FontWeight="SemiBold" />
        </StackPanel>

        <oxy:PlotView Grid.Row="1" Model="{Binding PlotModel}" />
    </Grid>
</UserControl>
