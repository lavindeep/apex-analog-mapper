namespace ApexMapper.App.Services;

/// <summary>UI-level calibration state badge for a discovered device row.</summary>
public enum DeviceCalibrationStatus
{
    Unknown,
    NotCalibrated,
    Calibrated,
    Faulted,
}
